# Backend Agent Memory — Nakiros

## Patterns & gotchas

- [project_skill_symlink_override_2026_05_13.md](project_skill_symlink_override_2026_05_13.md) — `skill-symlink-override.ts` added 2026-05-13. Atomic rename-based swap of `~/.claude/skills/<name>` for fix/edit-eval batches. `registerFixEvalBatch` now takes `onComplete?: () => void`. Both `fix:runEvalsInTemp` and `edit:runEvals` acquire on start, release via `onComplete` (batch end) or on `startEvalRuns` throw. Module-level `ACTIVE` map + `process.once` drain on shutdown.
- [feedback_shared_dist_stale.md](feedback_shared_dist_stale.md) — `@nakiros/shared` tsconfig uses `paths` pointing to sources, BUT stale `dist/index.d.ts` can still shadow them. Always run `pnpm -F @nakiros/shared build` when changing shared types before running `tsc --noEmit` on nakiros or frontend.
- [project_claudemd_scope_removed_2026_05_04.md](project_claudemd_scope_removed_2026_05_04.md) — `ClaudeMdScope` ('root'|'claude-dir'|'local') fully removed 2026-05-04. Only root CLAUDE.md is supported. Archive filename pattern changed from `audit-<scope>-<ISO>.md` to `audit-<ISO>.md`.
- [project_dot_claude_snapshot_v1_2026_05_04.md](project_dot_claude_snapshot_v1_2026_05_04.md) — `DotClaudeSnapshot` v1 shipped 2026-05-04. Builder at `services/dot-claude-snapshot-builder.ts` (sync, readFileSync). Wired in `prepareWorkdir` of audit/fix runners for claudemdTarget only. SKILL.md of claudemd-expert updated. `parseFrontmatter` helpers are local to the builder (not shared with claude-config-reader.ts — both files have private copies). `as typeof parsed` pattern fails for self-referential types — use a named type alias instead.
- [project_rules_expert_wired_2026_05_04.md](project_rules_expert_wired_2026_05_04.md) — `nakiros-rules-expert` wired 2026-05-04. New types: `RulesRunMode`, `RulesTargetContext` (project.ts), `RulesRunTarget` (agent-run.ts), `RuleSummary`/`RulesListResult`/etc. (claude-config.ts). 6 IPC channels `rules:*`. New files: `services/rules-audit-history.ts`, `daemon/handlers/rules.ts`. Runners: audit + fix both handle `rulesTarget` (snapshot write, first-prompt, archiving, no sandbox sync-back in fix finish). Frontend: `RunDock.tsx` needs explicit `if (target.type === 'rules')` guard before the `switch (target.scope)` to avoid TS2339 on `.scope`/`.pluginName`. Archive path: `~/.nakiros/<projectId>/rules-audits/<encodedRuleName>/audit-<ISO>.md` where `encodeRuleName` replaces `/` with `__`.
- [project_subagents_expert_created_2026_05_04.md](project_subagents_expert_created_2026_05_04.md) — `nakiros-subagents-expert` bundled skill created 2026-05-04 (pure skill, no daemon wiring). 15 checks across 5 sections (frontmatter/structure/config/content/crossref). Static script at `scripts/run-static-checks.mjs --subagent <path> --output-dir <path>` handles 11/15 deterministic checks (leaves 4 judgement: single_domain, imperative_mood, referenced_in_claudemd, skills_exist). Frontmatter parser handles block scalars (description: >) and inline arrays. Both project subagents (backend.md, frontend.md) correctly detected as missing `tools:` scoping.
- [project_subagents_expert_wired_2026_05_04.md](project_subagents_expert_wired_2026_05_04.md) — `nakiros-subagents-expert` wired 2026-05-04. New types: `SubagentsRunMode`, `SubagentsTargetContext` (project.ts), `SubagentsRunTarget` (agent-run.ts), `SubagentSummary`/`SubagentsListResult`/etc. (claude-config.ts). 6 IPC channels `subagents:*`. New files: `services/subagents-audit-history.ts`, `daemon/handlers/subagents.ts`. Runners: audit + fix both handle `subagentsTarget` (snapshot write, first-prompt, archiving, no sandbox sync-back in fix finish). Frontend: `RunDock.tsx` needs explicit `if (target.type === 'subagents')` guard before switch on scope (same pattern as rules). Archive path: `~/.nakiros/<projectId>/subagents-audits/<encodedSubagentName>/audit-<ISO>.md`. `RunSidePanel.tsx` has TWO separate `targetNoun` type declarations (line ~73 props + line ~367 inner FixPanel props) — both must include `'subagent'` to avoid TS2322 in RunScreen.tsx.
- [project_hooks_expert_created_2026_05_04.md](project_hooks_expert_created_2026_05_04.md) — `nakiros-hooks-expert` bundled skill created 2026-05-04 (pure skill, no daemon wiring yet). 14 checks across 4 sections (structure/handler/bp/crossref). Singleton audit: 1 audit = entire `hooks` block of `.claude/settings.json`. Static script `--settings <path> --output-dir <path>` handles 9/14 checks (leaves 5 for agent: bp.exit_code_2_for_blocking, bp.idempotent_or_documented, crossref.command_path_exists, crossref.referenced_subagent_exists, crossref.referenced_mcp_server_exists). Key difference vs other experts: hooks live in JSON not markdown, so no frontmatter parser needed. `Stop` event ignores `matcher` silently — check `structure.matcher_compatible_with_event`. `bp.no_unjustified_dangerous` partially deterministic (HTTP URL + path traversal) so included in static script.
- [project_hooks_expert_wired_2026_05_04.md](project_hooks_expert_wired_2026_05_04.md) — `nakiros-hooks-expert` wired 2026-05-04. Singleton pattern (mirrors claudemd). 4 IPC channels `hooks:*` (no list/delete). New types: `HooksRunMode`, `HooksTargetContext` (project.ts), `HooksRunTarget` (agent-run.ts), `HooksReadResult`/`HooksExpertMutationResult`/`HooksAuditHistoryEntry` (claude-config.ts). New files: `services/hooks-writer.ts` (read only hooks key, merge-save preserves all other keys, empty → removes key), `services/hooks-audit-history.ts`, `daemon/handlers/hooks.ts`. Archive path: `~/.nakiros/<projectId>/hooks-audits/audit-<ISO>.md` (no sub-folder). `RunDock.tsx` needs `if (target.type === 'hooks')` guard before `switch (target.scope)`. `run-display.ts` has new `isHooks` field. Do NOT confuse `hooks:*` channels with `claudeHooks:*` (Module 6 V2 editor).
- [project_permissions_expert_created_2026_05_04.md](project_permissions_expert_created_2026_05_04.md) — `nakiros-permissions-expert` bundled skill created 2026-05-04 (pure skill, no daemon wiring yet). 14 checks across 4 sections (structure/syntax/security/crossref). Static script handles 9/14 (leaves 5 for agent: dotclaude_writes_denied, env_files_denied, 3 crossref). Two critical checks: valid_json + default_mode_not_bypass. `dangerous_bash_denied` is info. No permissions block → all N/A except valid_json. `dontAsk` → dangerous_bash_denied N/A.
- [project_permissions_expert_wired_2026_05_04.md](project_permissions_expert_wired_2026_05_04.md) — `nakiros-permissions-expert` wired + scope toggle added 2026-05-04. 4 IPC channels `permissions:*`. **KEY**: `PermissionsExpertScope` (new) ≠ `PermissionsScope` (Module 4 exists). All IPC methods take `scope` arg. Archive: `~/.nakiros/<id>/permissions-audits/<scope>/audit-<ISO>.md`. PermissionsScreen: 2-pill toggle, handleScopeChange resets audit. ProjectOverview: 2-scope fetch + "X project · Y local" subtitle.
- [project_mcp_expert_created_2026_05_04.md](project_mcp_expert_created_2026_05_04.md) — `nakiros-mcp-expert` bundled skill created 2026-05-04 (pure skill, no daemon wiring yet). 14 checks across 4 sections. **KEY**: target is `.mcp.json` at project root (not inside `.claude/`). Static script handles 10/14 (leaves 4 crossref for agent). Absent file → valid_json:pass + 13 na. `server.env_no_plain_secrets` heuristic: keys matching `/(token|key|secret|password)/i` + value without `${...}` → fail.
- [project_mcp_expert_wired_2026_05_04.md](project_mcp_expert_wired_2026_05_04.md) — `nakiros-mcp-expert` wired 2026-05-04. Singleton pattern (mirrors hooks-expert). 4 IPC channels `mcp:*` (no list/delete). New types: `McpRunMode`, `McpTargetContext` (project.ts), `McpRunTarget` (agent-run.ts), `McpReadResult`/`McpExpertMutationResult`/`McpAuditHistoryEntry` (claude-config.ts). New files: `services/mcp-writer.ts` (reads+writes ENTIRE .mcp.json, no merge, empty → deletes file), `services/mcp-audit-history.ts`, `daemon/handlers/mcp.ts`. KEY difference vs hooks/permissions: target is complete file not sub-block. Archive path: `~/.nakiros/<projectId>/mcp-audits/audit-<ISO>.md` (no sub-folder). `RunDock.tsx` needs `if (target.type === 'mcp')` guard before `switch (target.scope)`. `run-display.ts` has new `isMcp` field. Do NOT confuse `mcp:*` channels with `claudeMcp:*` (Module 5 V2 editor).
- [project_output_styles_expert_created_2026_05_04.md](project_output_styles_expert_created_2026_05_04.md) — `nakiros-output-styles-expert` created 2026-05-04 (7th and final `.claude/` expert, pure skill no daemon wiring). 12 checks / 4 sections (frontmatter/structure/content/crossref). Static script `--style <path> --output-dir <path>` handles 8/12 (leaves 4 for agent: content.tone_specified, content.format_specified, crossref.*). No `paths:` field (output styles are not path-scoped). `content.role_defined` heuristic: `/(you are|tu es|act as|behave as|as a |as an )/i`. Imperative check: <= 2 hedges = pass. V1 project-scope only.
- [project_output_styles_expert_wired_2026_05_04.md](project_output_styles_expert_wired_2026_05_04.md) — `nakiros-output-styles-expert` wired 2026-05-04. Collection pattern (mirrors rules/subagents). 6 IPC channels `outputStyles:*`. **KEY GOTCHA**: Module 3 V2 éditeur déjà utilise `OutputStylesListResult` dans claude-config.ts — le type expert doit s'appeler `OutputStylesExpertListResult` pour éviter collision. Même pattern que `McpExpertMutationResult` vs `McpMutationResult`. New files: `services/output-styles-audit-history.ts`, `daemon/handlers/output-styles.ts`. `OutputStyleSummary` a `displayName` (from frontmatter `name:`) en plus des champs rules/subagents. Archive path: `~/.nakiros/<projectId>/output-styles-audits/<encodedStyleName>/audit-<ISO>.md`. `encodeStyleName`: `/` → `__`. `RunDock.tsx` needs `if (target.type === 'output-styles')` guard. `run-display.ts` has `isOutputStyles` field. `RunSidePanel.tsx` has TWO `targetNoun` type declarations — both need `'output style'`.
- [project_service_manager_2026_05_05.md](project_service_manager_2026_05_05.md) — `nakiros service install|start|stop|uninstall|status` shipped 2026-05-05. Module at `services/service-manager/`. launchd (macOS) + systemd (Linux). Key gotcha: `KeepAlive=true` means `stop` = SIGTERM but daemon restarts; use `uninstall` to kill permanently. `launchctl bootstrap gui/$UID` not deprecated `load`. npx cache guard in `paths.ts`.
- [project_edit_mode_wired_2026_05_07.md](project_edit_mode_wired_2026_05_07.md) — `edit` run kind wired 2026-05-07. `SkillAgentMode` += `'edit'`. Third mode in fix-runner; handlers/edit.ts mirrors fix.ts; 8 SKILL.md sections added. `edit:getBenchmarks`/`getEditsHistory`/`getEditTempMatrix` absent from 14-channel IPC spec — use `fix:*` equivalents with editRunId.
- [project_cowork_provider_wired_2026_05_07.md](project_cowork_provider_wired_2026_05_07.md) — Cowork provider wired 2026-05-07. `ProviderType` += `'cowork'`. New `cowork-scanner.ts`. `scan()` onProgress += `provider` arg. `ingestSession` has 4th arg `forcedProjectPath`. `ensureCoworkProjectIndexed(userDir, projectPath)` in ingest. Handler routing via `ensureIndexed(project)` helper.
- [project_apply_reco_noninteractive_2026_05_13.md](project_apply_reco_noninteractive_2026_05_13.md) — `<apply-recommendation>` fix (2026-05-13 round 2): block now embedded in FIRST PROMPT via `ApplyRecommendationContext` on `StartAuditRequest` + `buildNonInteractiveApplyPrompt` in fix-runner. `ReqBuilder` takes 4th arg `brief`. No more `sender` call in `applyReco`. `resolveApplyTarget` lookup table covers 8 artefact types. tsc OK on both nakiros + frontend.
