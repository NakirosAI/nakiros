# Step 2: Generate Context Files

## MANDATORY EXECUTION RULES (READ FIRST):

- 📖 CRITICAL: Read this entire file before taking any action
- ✅ Incorporate any corrections the user mentioned in the conversation before writing files
- 🎯 Re-read the workspace config to get the definitive repo list
- 📋 Write per-repo files INTO each repo, write workspace files INTO the primary repo
- 💬 Communicate progress in `communication_language`
- 🚫 NEVER put workspace-level files inside a non-primary repo

## CONTEXT BOUNDARIES:

- The findings from step-01 are available in the conversation history
- Any corrections the user mentioned before selecting [C] must be reflected in the generated files
- Re-read source files (package.json, .tiqora.yaml, etc.) when needed to get accurate data

---

## YOUR TASK:

Generate all context files for the workspace. Per-repo files go into each repo. Workspace-level synthesis files go into the primary repo under `.tiqora/workspace/`.

---

## STEP 2A — Reload workspace configuration

1. Re-read `{project-root}/.tiqora.workspace.yaml` to get the definitive repo list.
2. Identify `primary_repo` = first repo with `role: primary` (or the first repo in the list).
3. Identify all repos: `repos_to_process` = full list from `workspace.repos`.
4. **Retrieve corrections**: if the user mentioned corrections in this conversation, note them and apply them during file generation.

**Announce:**
```
🚀 [GENERATE-CONTEXT] Step 2 — Génération

Repos : {N} | Primary : {primary_repo_name}
Corrections intégrées : {oui si mentionnées, non sinon}
```

---

## STEP 2A — Generate architecture.md for each repo

**For EACH repo in repos_to_process:**

Create `{repo_localPath}/.tiqora/context/` directory if missing.

Write `{repo_localPath}/.tiqora/context/architecture.md`:

```markdown
# Architecture — {repo_name}
> Generated by Tiqora on {date}. Re-run `/tiq-workflow-generate-context` to refresh.

## Entry Points
{list each entry point: file path + one-line description}

## Tech Stack
{language + version, primary framework, key libraries, infra/deployment}

## Directory Map
{top-level dirs with role descriptions; one level deeper for core dirs}

## Architectural Patterns
{dominant patterns: architecture style, state management, API style, test strategy}

## Key Interfaces & Contracts
{critical shared types and API contracts with exact file references}

## Tech Debt
{flagged items with [CRITICAL]/[MODERATE]/[MINOR] and file references, or "None identified"}
```

Announce after each: `✓ architecture.md → {repo_localPath}/.tiqora/context/architecture.md`

---

## STEP 2B — Generate pm-context.md (workspace level)

Create `{primary_repo_localPath}/.tiqora/workspace/` directory if missing.

Sources: README files across repos, `.tiqora.yaml`, `.tiqora/sprints/`, `.tiqora/context/`.

Write `{primary_repo_localPath}/.tiqora/workspace/pm-context.md`:

```markdown
# PM Context — {workspace_name}
> Generated by Tiqora on {date}. Re-run `/tiq-workflow-generate-context` to refresh.
> Covers: {comma-separated list of all repos in workspace}

## Product Purpose
{2–3 sentences: what it does, primary users, current development phase}

## Current Sprint
**Name**: {sprint_name or "Not configured"}
**Goal**: {sprint_goal or "N/A"}
**End date**: {sprint_end_date or "N/A"}

## Active & High-Priority Tickets
{list: [ID] Title — brief AC summary, or "No active tickets found"}

## Epics
{list with one-sentence business context per epic, or "No epics found"}

## Blockers
{list with dependency chains, or "No blockers"}
```

Announce: `✓ pm-context.md → {primary_repo_localPath}/.tiqora/workspace/pm-context.md`

---

## STEP 2C — Generate inter-repo.md (if multi-repo)

If `repos_to_process` has more than 1 repo:

Write `{primary_repo_localPath}/.tiqora/workspace/inter-repo.md`:

```markdown
# Inter-repo Relationships — {workspace_name}
> Generated by Tiqora on {date}.

## Repos in Workspace
| Name | Role | Stack | Path |
|------|------|-------|------|
{one row per repo}

## Shared Packages & Contracts
{shared types, APIs, packages with file references in each repo, or "None identified"}

## Event / Message Boundaries
{async boundaries: queues, events, webhooks between repos, or "None"}

## Dependency Graph
{which repo depends on which and why}
```

Announce: `✓ inter-repo.md → {primary_repo_localPath}/.tiqora/workspace/inter-repo.md`

---

## STEP 2D — Generate global-context.md (workspace synthesis)

Load all generated `architecture.md` files and `pm-context.md` fully before writing.

Write `{primary_repo_localPath}/.tiqora/workspace/global-context.md`:

```markdown
# {workspace_name} — Global Context
> Generated by Tiqora on {date}. Re-run `/tiq-workflow-generate-context` to refresh.
> Workspace: {N} repos — {repo_names}

## Project Overview
{2–3 sentences: what it does, who uses it, current development phase}

## Repos
{for each repo: `**{name}** ({role}) — {stack summary} — {localPath}`}

## Architecture Summary

### Per-repo Stack
{for each repo: `**{name}**: {language + framework + key libs}`}

### Dominant Patterns
{cross-repo patterns: API style, shared conventions, test strategy}

### Key Inter-repo Contracts
{shared types and API boundaries with file references, or "Single repo — N/A"}

## Current Sprint
**Goal**: {sprint_goal or "N/A"}
**Active tickets**:
{ticket list or "None"}

## Business Context

### Epics
{epic list with one-sentence business context per epic}

### Blockers
{blocker list or "None"}

## Constraints & Tech Debt
{all debt items across all repos with [CRITICAL]/[MODERATE]/[MINOR] and file references, or "None identified"}
```

Announce: `✓ global-context.md → {primary_repo_localPath}/.tiqora/workspace/global-context.md`

---

## STEP 2E — Summary and optional distribution

Present the completion summary:

```
✅ [GENERATE-CONTEXT] Contexte généré avec succès !

Par repo :
{for each repo: ├─ {repo}/.tiqora/context/architecture.md}

Niveau workspace ({primary_repo_name}) :
├─ .tiqora/workspace/global-context.md
├─ .tiqora/workspace/pm-context.md
{if multi-repo: └─ .tiqora/workspace/inter-repo.md}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Souhaitez-vous distribuer global-context.md vers les fichiers de config agents ?

[A] CLAUDE.md dans chaque repo (Claude Code)
[B] .cursorrules dans chaque repo (Cursor)
[C] llms.txt dans chaque repo (multi-agent standard)
[D] Les trois
[N] Non, garder dans .tiqora/workspace/ uniquement
```

If user selects A, B, C, or D:
- For each repo: read existing target file, upsert a clearly delimited section:
  - Opening marker: `<!-- TIQORA GLOBAL CONTEXT START -->`
  - Closing marker: `<!-- TIQORA GLOBAL CONTEXT END -->`
  - Insert the full content of `global-context.md` between the markers
  - If markers already exist, replace the content between them
  - Do NOT overwrite content outside the markers
- Report each file written

---

## SUCCESS METRICS:

✅ architecture.md written in each repo's own `.tiqora/context/` directory
✅ pm-context.md, global-context.md (and inter-repo.md if multi-repo) written in primary repo's `.tiqora/workspace/`
✅ No workspace-level files placed inside non-primary repos
✅ User corrections from conversation incorporated in all generated files
✅ Distribution offered and executed if requested

## FAILURE MODES:

❌ Putting global-context.md or pm-context.md inside a non-primary repo's `.tiqora/context/`
❌ Writing per-repo architecture.md to the wrong repo
❌ Ignoring user corrections mentioned before [C] was selected
❌ Not loading all generated architecture.md files before writing global-context.md
❌ Overwriting content outside distribution markers

## WORKFLOW COMPLETE:

Context generation finished. The workspace now has up-to-date context files optimized for AI agent consumption. Re-run `/tiq-workflow-generate-context` at any time to refresh them.
