name: dev-story
description: "Execute a Nakiros story/ticket with config merge, challenge gate, implementation, validation, and run-state tracking"
author: "Nakiros"

# Workflow engine bootstrap (required by ~/.nakiros/core/tasks/workflow.xml)
config_source: "{project-root}/.nakiros.yaml"
date: system-generated

# Workflow components
installed_path: "{project-root}/~/.nakiros/workflows/4-implementation/dev-story"
instructions: "{installed_path}/instructions.xml"
validation: "{installed_path}/checklist.md"

# Runtime paths
project_config: "{project-root}/.nakiros.yaml"
profile_config: "~/.nakiros/config.yaml"
active_run_file: "{project-root}/.nakiros/state/active-run.json"
workflow_runs_dir: "{project-root}/.nakiros/workflows/runs"
workflow_steps_dir: "{project-root}/.nakiros/workflows/steps"

# PM tracking defaults used by instructions.xml
pm_in_progress_status: "In Progress"
pm_review_status: "In Review"
pm_done_status: "Done"
pm_blocked_status: "Blocked"
worklog_rounding_minutes: 15
minimum_worklog_minutes: 15

# Defaults applied by instructions.xml after config merge
default_user_name: "Developer"
default_idle_threshold_minutes: 15
default_communication_language: "Français"
default_document_language: "English"
