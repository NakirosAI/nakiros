/** Fixed sidebar rail width (px). */
export const SIDEBAR_WIDTH = 68;
/** Ticket detail drawer width (px). */
export const TICKET_DETAIL_WIDTH = 560;
/** Right-hand context panel width (px). */
export const CONTEXT_PANEL_WIDTH = 320;
/** Default kanban column width (px). */
export const KANBAN_COLUMN_WIDTH = 260;
/** Settings nav rail width (px). */
export const SETTINGS_NAV_WIDTH = 160;
