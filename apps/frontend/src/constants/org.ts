/**
 * Window-level CustomEvent name dispatched when the org/preferences state
 * changes. Listeners refresh themselves without a full reload.
 */
export const ORG_STATE_CHANGED_EVENT = 'nakiros:org-state-changed';
