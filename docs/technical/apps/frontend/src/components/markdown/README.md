# markdown/

**Path:** `apps/frontend/src/components/markdown/`

Reusable markdown authoring components. Currently contains the shared `MarkdownEditor` (Milkdown Crepe WYSIWYG + Raw toggle) consumed by every `.claude/` entity Edit tab.

## Files

- [MarkdownEditor.tsx](./MarkdownEditor.md) — Controlled WYSIWYG/Raw markdown editor wrapping Milkdown Crepe; toggle is internal, parent receives plain markdown strings.
