# landing/

**Path:** `apps/landing/`

`@nakiros/landing` — the Nakiros marketing landing page. Vite + React + TypeScript SPA, static-only, independent from the in-app web UI in `apps/frontend`. Deployed as a standalone bundle.

## Subfolders

- [src/](./src/README.md) — Application source (App, sections, i18n, lib).
