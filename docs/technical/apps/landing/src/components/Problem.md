# Problem.tsx

**Path:** `apps/landing/src/components/Problem.tsx`

Renders the "Problem" section (section 01, v2 landing layout) — four stat cards illustrating the unmanaged `.claude/` problem. Each card has a large numeric key, a label, and a short description. Icons and accent tones are driven by `p.tone`. All copy from `messages.problem.pillars`.

## Exports

### `Problem`

```ts
export function Problem(): JSX.Element
```

"Problem" section — four stat cards illustrating the unmanaged `.claude/` problem faced by Claude Code teams. Occupies section 01 of the v2 landing layout.

Each card shows a large numeric key (`p.k`), a label, and a short description. Icon and accent tone are driven by `p.tone`. All copy comes from `messages.problem.pillars`.
