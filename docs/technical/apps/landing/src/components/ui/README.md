# ui/

**Path:** `apps/landing/src/components/ui/`

UI primitives specific to the landing page. Kept duplicated from `apps/frontend/src/components/ui/` so the landing bundle stays minimal and free of frontend dependencies.

## Files

- [Button.tsx](./Button.md) — Shared button primitive with focus-visible ring, three Tailwind variants, and `tailwind-merge`-aware composition.
