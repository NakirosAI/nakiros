# i18n/

**Path:** `apps/landing/src/i18n/`

Locale provider and `useI18n` hook for the landing page. EN/FR JSON bundles live next to this file in `./locales/`.

## Files

- [I18nProvider.tsx](./I18nProvider.md) — `Locale` / `Messages` types, `I18nProvider` (detects/persists locale), and the `useI18n` hook every section uses.
