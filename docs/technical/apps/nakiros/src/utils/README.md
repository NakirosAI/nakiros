# utils/

**Path:** `apps/nakiros/src/utils/`

Cross-cutting utilities used by the daemon and the service layer. Currently only holds the `~/.nakiros/` path helpers.

## Files

- [nakiros-dir.ts](./nakiros-dir.md) — Resolve `~/.nakiros/` + build absolute paths inside it.
