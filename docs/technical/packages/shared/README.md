# shared/

**Path:** `packages/shared/`

`@nakiros/shared` — the shared-types + IPC-channels package. Every type that crosses the wire between the daemon and the frontend, plus the canonical channel registry, lives here so the two sides cannot drift.

## Subfolders

- [src/](./src/README.md) — Source root: runtime constants, IPC channel registry, and shared types.
