name: plan-feature
description: "Slice a feature into stories and architect-reviewed tasks. Writes story files to features/{slug}/stories/ and updates the feature's stories table."
author: "Nakiros"

# Workflow engine bootstrap
config_source: "~/.nakiros/config.yaml"
date: system-generated

# Workflow components
installed_path: "~/.nakiros/workflows/2-pm/plan-feature"
instructions: "{installed_path}/instructions.xml"

# Runtime paths
profile_config: "~/.nakiros/config.yaml"
workspace_config: "workspace.yaml"
workspace_root_dir: "~/.nakiros/workspaces/{workspace_slug}"
workspace_context_root: "{workspace_root_dir}/context"
features_root: "{workspace_context_root}/features"
feature_dir: "{features_root}/{{feature_slug}}"
stories_dir: "{feature_dir}/stories"

# Defaults
default_user_name: "Developer"
default_communication_language: "Français"
default_document_language: "English"
