name: hotfix-story
description: "Fast-track production incident workflow: triage, minimal-scope fix, expedited MR, production sync"
author: "Nakiros"

# Workflow engine bootstrap
config_source: "{project-root}/.nakiros.yaml"
date: system-generated

# Workflow components
installed_path: "{project-root}/~/.nakiros/workflows/4-implementation/hotfix-story"
instructions: "{installed_path}/instructions.xml"

# Runtime paths
project_config: "{project-root}/.nakiros.yaml"
profile_config: "~/.nakiros/config.yaml"
workspace_config: "{project-root}/.nakiros.workspace.yaml"
active_run_file: "{project-root}/.nakiros/state/active-run.json"
workflow_runs_dir: "{project-root}/.nakiros/workflows/runs"

# PM tracking
pm_in_progress_status: "In Progress"
pm_review_status: "In Review"
pm_done_status: "Done"
worklog_rounding_minutes: 15
minimum_worklog_minutes: 15

# Hotfix-specific
hotfix_branch_prefix: "hotfix/"

# Defaults
default_user_name: "Developer"
default_communication_language: "Français"
default_document_language: "English"
