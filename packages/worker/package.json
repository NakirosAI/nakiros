{
  "name": "@nakiros/worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit false --declaration false --skipLibCheck --module es2022 --target es2022 --moduleResolution bundler --outDir src",
    "deploy": "pnpm build && wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "type-check": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply nakiros-api --local",
    "db:migrate": "wrangler d1 migrations apply nakiros-api"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "drizzle-orm": "^0.45.1",
    "resend": "^6.9.3",
    "tslib": "^2.8.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250224.0",
    "drizzle-kit": "^0.31.9",
    "typescript": "^5.0.0",
    "wrangler": "^4.71.0"
  }
}
